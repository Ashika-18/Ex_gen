
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" crossorigin="anonymous" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="/javascripts/main.js" defer></script>
</head>
<body class="container">
    <header>
        <h1 class="display-4"><%= title %></h1>
    </header>
    <div role="main">
        <p>これはboardsのindexです!</p>
        <p class="h4"> Welcome <%= login.name %>さん</p>
        <form action="/boards/edit" method="post">
            <div class="row">
                <div class="col-10">
                    <input type="text" name="msg" class="form-control" value="<%= board.content %>">
                </div>
                <input type="submit" value="編集" class="btn btn-success col-2">
            </div>
        </form>

        <table class="table mt-5">
            <% for (let i in content) { %>
            <%- include('data_item', { val: content[i] }) %>
            <% } %>
        </table>
    </div>
</body>
</html>